# Claude Instructions

Follow `AGENTS.md` as the source of truth for this repository.

Key reminders:

- Kotlin + Spring Boot 3.
- REST first.
- PostgreSQL + Flyway.
- Spring Data JPA.
- Local JWT auth first; OAuth optional later.
- HMAC and AES-GCM are optional security modules.
- This repo has AI coding-agent setup. Do not add runtime AI modules unless explicitly requested.
- Add or update tests with behavior changes.
- Never commit secrets.
